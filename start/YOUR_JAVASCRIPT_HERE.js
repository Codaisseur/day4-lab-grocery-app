// Write your JS here

function createGroceryItemElement(){
  // Stuff you'll need:
  // document.createElement
  // element.setAttribute (to set the for attribute on labels for example)
  // element.addEventListener 
  // element.innerText
  // element.appendChild 
}

function toggleBought(){
  // hint: 
  // - pass the event to toggleBought as an argument so you can select the checkbox as event.target 
  // OR
  // - Use .bind(this) so you can access the checkbox as 'this' (as in the todo example)
}

function submitGroceryItem(){
  // Stuff you'll need:
  // document.getElementById (to select the inputfields)
  // calling the function createGroceryItemElement
  // element.appendChild
}
